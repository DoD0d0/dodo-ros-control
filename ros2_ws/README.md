# ROS 2 Workspace

This is the ROS 2 workspace used inside the Docker container.

All ROS 2 packages for Dodo sim2sim / sim2real control are located under `src/`.

ROS distribution (container): Humble
