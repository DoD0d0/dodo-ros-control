# Local Policy Models

This directory stores trained RL policy files used at runtime.

- Policy files are NOT tracked by git
- Models must be downloaded locally
- The path is provided via ROS parameters