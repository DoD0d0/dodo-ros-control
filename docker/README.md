# Docker Runtime

This directory contains the Docker runtime for ROS 2 Humble.

- Ubuntu 22.04
- ROS 2 Humble
- Headless runtime for sim2sim and sim2real experiments
- Base image: `osrf/ros:humble`

The container mounts the ROS workspace, assets, and local policy models.
